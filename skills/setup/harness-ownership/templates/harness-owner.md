# ハーネスの所有と見直し

- DRI（直接責任者）: <名前 / 連絡先>。CLAUDE.md の階層、フック、権限、プラグインの構成を決め、最新に保ちます。
- 前回の構成レビュー: <YYYY-MM-DD>
- 次回の構成レビュー: <YYYY-MM-DD>（前回から3〜6か月後。主要なモデルのリリース後に性能が頭打ちだと感じたら前倒し）

## レビューで見ること

- `/harness-audit` を実行し、前回の採点表と比べる。
- 旧モデルの制約回避のために入れたフックや指示で、今は不要になったものを外す。
- CLAUDE.md の各ファイルが 200 行未満か。手順が残っていればスキルへ。
- `.claude/harness-kit/claudemd-candidates.md` に溜まった追記候補を処理する。

## 変更履歴

| 日付 | 変更 | 理由 |
|---|---|---|
| <YYYY-MM-DD> | ハーネス初期構築 | cc-harness-kit で導入 |
