# <パッケージ / サブシステム名>

<この領域が何かを1文で。ルートの CLAUDE.md と重複することは書かない。>

## 環境の注意点
- <この領域でだけ起きる罠。起動手順、環境変数、ポート、外部サービス>

## 判断の基準
- <この領域の規約のうち、ツールの既定と異なるもの>

## 実行前に確認を取る操作
- <この領域固有の不可逆操作。migration、契約ファイル、生成コード>

## 必要時に読むもの
- <この領域のスキル。`.claude/skills/<名前>/` に置く>
