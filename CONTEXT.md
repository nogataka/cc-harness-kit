# cc-harness-kit の語彙

このリポジトリで使う言葉の定義です。スキル本文、ADR、docs はこの語彙で書きます。

## 用語

**ハーネス**:
モデルを取り巻く仕組みの総称。Anthropic は CLAUDE.md、フック、スキル、プラグイン、MCP サーバを「5つの拡張点」、LSP とサブエージェントを「2つの追加機能」と呼びます。このリポジトリでは構築順に並べてまとめて **7層** と呼びます。
_避ける_: 設定一式、環境（曖昧なため）

**層**:
ハーネスを構築順に分けた単位。第1層 CLAUDE.md、第2層 フック、第3層 スキル、第4層 プラグイン、第5層 LSP、第6層 MCP、第7層 サブエージェント。各層は前の層を土台にします。

**対象リポ**:
スキルを適用する先のリポジトリ。このリポジトリ自身ではありません。

**起動ディレクトリ**:
対象リポで `claude` を起動したディレクトリ。`.claude/settings.json` はこのディレクトリのものだけが読まれ、親から継承されません。生成する設定はここに置きます。
_避ける_: プロジェクトルート（起動ディレクトリと一致しない場合があるため）

**提案書**:
スキルが対象リポに書き込む前に出力する変更案のファイル。`harness-proposal-<層>.md` の名前で起動ディレクトリに置きます。人が承認した項目だけを書き込みます。

**譲れないもの**:
CLAUDE.md の文章で誘導するのではなく、フックで機械的に強制する対象。フォーマット、テスト実行、セキュリティチェック、不可逆操作の確認など。

**監査**:
`harness-audit` が行う現状の採点。7層それぞれについて「充足 / 一部 / 未着手」を判定します。書き込みは行いません。

**DRI**:
Directly Responsible Individual。対象リポのハーネス構成を所有し、最新に保つ責任を持つ1人。

## 関係

- ハーネスは7つの層から成る
- 各層に1つの setup スキルが対応する（第6層 MCP と第7層 サブエージェントは案内のみで、専用スキルは持たない）
- 監査は7層すべてを採点し、`harness-init` はその結果を見て未充足の層の setup スキルへ案内する
- 提案書は setup スキルごとに1つ作られ、承認後に消してよい

## 曖昧さの解消

- 「設定」は何を指すか曖昧でした。`.claude/settings.json` を指すときは **設定ファイル**、CLAUDE.md を含む全体を指すときは **ハーネス** と書きます。
- 「除外」は `.gitignore` による検索除外と `permissions.deny` による読み取り拒否の2つを指していました。前者を **検索除外**、後者を **読み取り拒否** と書き分けます。
